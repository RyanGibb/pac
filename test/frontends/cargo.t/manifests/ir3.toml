[package]
name = "ir3"
version = "1.0.0"
edition = "2015"
resolver = "3"

[lib]
path = "src/lib.rs"

[features]


[dependencies]
  i3 = { version = "1", default-features = false, features = ["net"] }

