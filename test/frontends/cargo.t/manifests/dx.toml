[package]
name = "dx"
version = "1.0.0"
edition = "2015"
resolver = "3"

[lib]
path = "src/lib.rs"

[features]


[dependencies]
  sb = { version = "=2.6.1" }
  tr = { version = "^1", features = ["url"] }

