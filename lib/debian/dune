(library
 (name debian_frontend)
 (modules deb_version deb_packages apt_args)
 (libraries version))

(library
 (name deb_solve)
 (modules tables order deb_solve)
 ; dune's warnings, reporting rather than failing: a warning here is a
 ; note to the author, and only the extraction this links against needs
 ; them off wholesale.
 (flags
  (:standard -warn-error -a))
 (libraries debian_frontend pac pubgrub unix))

(test
 (name test_version)
 (modules test_version)
 (libraries debian_frontend))

(test
 (name test_apt_args)
 (modules test_apt_args)
 (libraries debian_frontend))
